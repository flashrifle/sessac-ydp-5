<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <%- include('./include/head', {title: '홈'}) %>

        <link rel="stylesheet" href="/public/css/home.css" />
    </head>
    <body>
        <%- include('./include/header') %>
        <h1>Hello! Express!!</h1>
        <p>안녕하세요~~~~~ <%= userId %>님! 환녕합니다</p>
        <p><%= userId %> 님의 비밀번호는 <%= userPw %> 입니다</p>
        <% for (let i = 0; i < 5; i++) { %>
        <div>하이하이!!!!!!!!</div>
        <% } %> <% for (let btn of btns) {%>
        <button type="button"><%= btn %></button>
        <%}%>

        <br />
        <span><%= me.name%></span>
        <span><%= me.msg%></span>

        <br />
        <% if(!isLogin) { %>
        <h4>아직 로그인을 하지 않았군요</h4>
        <% } else { %>
        <h4>로그인을 하셨군요</h4>
        <%}%>
    </body>
</html>
